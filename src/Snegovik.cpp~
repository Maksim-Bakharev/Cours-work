#include <graphics.h>
#include <stdio.h>
#include <conio.h>
#include <math.h>
#include <iostream>
#include <stdlib.h>
#include <time.h>
#include <locale.h>
#include <ctime>
#include <fstream>


void snegovik (int kx, int ky, int color1, int color2)
{
     setcolor(color1);
     setfillstyle ( 1, color2 );
     circle( kx, ky, 10);
     circle (kx, ky+25, 15);
     circle (kx-4, ky-1, 3 );
     circle (kx+4, ky-1, 3 );
     circle (kx, ky+4, 3 );
     circle (kx, ky+15, 3 );
     circle (kx, ky+23, 3 );
     circle (kx, ky+31, 3 );
     floodfill(kx,ky,color1); 
}
